# Track the quantized streaming model via Git LFS so a fresh clone can
# run `npm run test:streaming` without a manual download step.
web/frontend/public/fastconformer_phoneme_q8.onnx filter=lfs diff=lfs merge=lfs -text
